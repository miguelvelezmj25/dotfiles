" Syntax highlighting and indentation
filetype plugin indent on
syntax on

" Display numbers
set number

" Show a line at the position 100 chars to know limit line length
set colorcolumn=100

" Show position info
set ruler

" Show current command
set showcmd

" When the tab key is presed, insert 4 spaces
set tabstop=4

" When indenting with '>', use 4 spaces
set shiftwidth=4

" Insert spaces when the tab key is pressed
set expandtab

" Change all existing tab character to match the current tab settings
retab
